export interface MyEvent {
    end?: string;
    start: string;
    id: string;
    title: string;
    allDay?: boolean;
    constraint?: string;
    backgroundColor?: string;
    borderColor?: string;
}
