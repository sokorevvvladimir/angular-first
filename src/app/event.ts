export interface MyEvent {
    end?: string,
    start: string,
    id: string,
    title: string,
    allDay?: boolean,
    constraint?: boolean,
    backgroundColor?: string,
    borderColor?: string
}